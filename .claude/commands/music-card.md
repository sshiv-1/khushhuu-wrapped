Create or update a music memory card for One Year.

Details: $ARGUMENTS

Card should show:
- Album art with fallback for missing images
- Song title in serif
- Artist in small muted text
- Date in same muted small style
- Italic serif quote — the memory (use TODO if not provided)
- Small circular icon bottom-right
- Subtle gray tab at card top (matches existing design)

Grid: 3-column, staggered entrance on scroll.
Hover: translateY -2px, shadow slightly stronger. Subtle only.